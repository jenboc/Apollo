namespace Apollo.MIDI;

public static class MidiWriter
{
    /// <summary>
    /// Checks if a path is valid, it will create the necessary directories if it is valid, but doesn't exists
    /// </summary>
    /// <param name="path">The path to validate/create</param> 
    /// <returns>A boolean which depicts whether the operation was successful or possible</returns>
    private static bool PathIsValid(string path)
    {
        
    }
}